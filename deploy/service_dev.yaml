apiVersion: v1
kind: Service
metadata:
  name: promotions-namespace-dev
spec:
  selector:
    app: promotions-namespace-dev
  type: NodePort
  internalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 8080
      nodePort: 31001
      targetPort: 8080